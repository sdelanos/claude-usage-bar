import SwiftUI

extension Color {
    /// Claude's brand blue — the hue claude.ai uses for usage bars. Defined in
    /// one place so any future tweak to match a brand update is a single-file
    /// change.
    static let claudeBlue = Color(red: 0.235, green: 0.357, blue: 0.898)
}
